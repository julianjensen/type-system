const 	{
    writable,
    readable,
    x: [,abc],
    s: { t }
}: {
    writable: WritableStream<R>,
    readable: ReadableStream<T>,
    x: [ TypeOne, TypeTwo ],
    s: {
        t: number
    }
};

/*
const 	{
    writable,
    readable,
    x: [,abc],
    s: {t}
}: { writable: WritableStream<R>, readable: ReadableStream<T>, x: [ abc, def ], s: {t: 123} };


 */